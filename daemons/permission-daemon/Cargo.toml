[package]
name = "xos-permission-daemon"
version = "0.1.0"
edition = "2021"
description = "XOS Permission Management Daemon"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
chrono = "0.4"

[profile.release]
lto = true
codegen-units = 1
